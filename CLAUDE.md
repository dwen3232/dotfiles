# Dotfiles Repository

## Stow Setup
`just stow-configs` symlinks `dot-*` directories to `~/.*` using GNU Stow.
When asked to update claude configurations, ALWAYS assume that `./dot-claude/` is EQUIVALENT to `~/.claude/`
