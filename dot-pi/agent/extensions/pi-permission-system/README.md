# Pi Permission System Config

Global policy for `@gotgenes/pi-permission-system`.

## TODO

- Figure out a worktree-aware `external_directory` strategy. Current allowances cover known repository roots under `~/Repositories`, but project worktrees may live outside those paths or use generated directory names that need explicit allow patterns.
